"use strict";
hooks.addListener("onMapLoad", function () {
    /*
    let component = ui.createComponent(() => {
        return new ui.Canvas().addChild(new ui.Text("text"))
    }, {})
    component.construct();
    */
    console.log(typeof UnityEngine);
});
